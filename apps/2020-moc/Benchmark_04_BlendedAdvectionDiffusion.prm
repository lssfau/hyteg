Parameters
{
  level 5;
  numTimeSteps 63;
  threeDim false;

  // tStart will be set to keep
  // tStart * diffusivity = 1e-3 * 0.5 * pi
  // if diffusivity is negative, MOC w/o re-init is used
  diffusivity 1e-3;

  adjustedAdvection false;
  resetParticles false;
  rotationOnly false;

  vtk true;
  printInterval 1;
  vtkInterval 10;
  verbose false;
  dbFile Benchmark_04_BlendedAdvectionDiffusion.db;

}
