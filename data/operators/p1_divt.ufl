# generate code with ffc p1_divt.ufl
element = FiniteElement("Lagrange", triangle, 1)

p = TrialFunction(element)
v = TestFunction(element)
p, v = v, p # HHG uses FORTRAN layout

forms = [- v.dx(i)*p*dx for i in xrange(2)]
