# generate code with ffc p1_div.ufl
element = FiniteElement("Lagrange", triangle, 1)

u = TrialFunction(element)
q = TestFunction(element)
u, q = q, u # HHG uses FORTRAN layout

forms = [-u.dx(i)*q*dx for i in xrange(2)]
