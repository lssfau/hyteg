# generate code with ffc p1_to_bubble_div.ufl
p1 = FiniteElement("Lagrange", triangle, 1)
bubble = FiniteElement("Bubble", triangle, 3)

u = TrialFunction(bubble)
q = TestFunction(p1)

forms = [-u.dx(i)*q*dx for i in range(2)]
